# PROMPT.md — 프롬프트 이력

이 프로젝트를 만들 때 사용한 **개발 프롬프트**(사람 → 코딩 에이전트)와, 앱이 실행 중에 Claude 에게
보내는 **런타임 프롬프트**(앱 → Claude API)를 함께 누적 기록합니다.
프롬프트를 고칠 때는 코드만 바꾸지 말고 **이 문서의 해당 항목과 맨 아래 변경 이력에 함께 반영**하세요.

- 런타임 프롬프트의 원본(단일 진실 공급원)은 코드입니다. 이 문서는 **전문 + 그렇게 쓴 이유**를 남깁니다.
- 표기: 🧑 개발 프롬프트 / 🤖 런타임 프롬프트

---

## 1. 개발 프롬프트 이력 🧑

### P1 — 1단계: 쇼츠 크롤러 에이전트 구현 (2026-08-02)

원문:

>### Role & Goal
>당신은 유튜브 쇼츠 주력 채널의 데이터 수집, 댓글 기반 구독자 반응 분석, 및 웹 대시보드 시각화를 위한 유튜브 쇼츠 크롤링 에이전트 개발 전문가입니다.
>일반 롱폼 영상은 분석 대상에서 제외하고, YouTube Data API v3를 활용해 쇼츠 콘텐츠 및 댓글/대댓글 반응 수집에 특화된 로직을 구현하세요. 로컬 웹 브라우저에서 실행 가능한 Docker 기반 에이전트 코드를 작성해 주세요.
>
>**[1단계] 기능 구현**
>
>1. 에이전트 도구 및 쇼츠 전용 최적화 수집 프로세스
>* youtube_channel_crawler 도구:
>롱폼 영상 분석을 제외하고 60초 이하 쇼츠 콘텐츠 및 댓글/대댓글 수집에 집중하여 쿼터 소모를 절감합니다.<br>
> 수집 프로세스 및 API 호출 순서
> * channels.list (part=snippet,statistics,contentDetails,brandingSettings)
>   * 채널 프로필(채널명, 설명, 썸네일, 개설일), 통계(구독자 수, 총 조회수, 총 쇼츠 수) 수집
>   * contentDetails.relatedPlaylists.uploads에서 업로드 재생목록 ID(uploads ID) 추출
> * playlistItems.list (part=snippet,contentDetails)
>   * 최근 업로드 영상 ID 리스트(videoId) 수집
> * videos.list (part=snippet,statistics,contentDetails)
>   * 재생시간(duration)을 확인하여 60초 이하인 쇼츠 영상만 필터링 및 수집 (60초 초과 롱폼은 분석 대상에서 완전히 제외)
>   * 쇼츠 메타데이터 및 성과 지표(조회수, 좋아요 수, 댓글 수) 수집
> * commentThreads.list (part=snippet,replies) - 핵심 반응 분석 단계
>   * 수집된 각 쇼츠 영상별 주요 댓글 및 대댓글(Replies) 스레드 전체 수집
>   * 수집된 댓글 데이터를 기반으로 LLM이 시청자 반응 요약, 긍정/부정 감정 분석, 반복되는 키워드/요구사항 추출 진행
> * get_crawling_results 도구:
수집된 쇼츠 채널 메타데이터, 쇼츠 성과 순위, 시청자 댓글/대댓글 분석 종합 데이터를 구조화(JSON)하여 반환합니다.
> 2. 웹 대시보드 UI 구현 (사용자 가시성 최적화)
> * Streamlit (또는 Gradio) 기반 웹 대시보드를 구동하여 쇼츠 채널 분석 결과를 직관적으로 시각화:
>   * 상단 핵심 메트릭 카드: 구독자 수, 총 조회수, 수집 쇼츠 평균 조회수, 평균 좋아요 수/비율
>   * 성과 최상위 쇼츠 (Top Shorts) 대시보드:
조회수 및 좋아요가 가장 높은 인기 쇼츠 랭킹 카드 (세로형 썸네일 표출)
>   * 쇼츠 시청자 댓글 & 대댓글 반응 분석 섹션:
LLM이 요약한 쇼츠 시청자 주요 반응 키워드 및 피드백 리포트
가장 높은 공감을 받은 주요 댓글 및 대댓글 스레드 상세 보기
>   * 데이터 시각화 차트: 최근 쇼츠별 조회수 vs 좋아요 상관관계 차트
> 3. Docker 환경 구성
> * 로컬에서 손쉽게 실행할 수 있도록 Dockerfile 및 docker-compose.yml을 작성하세요.
> * 웹 UI 포트 바인딩(예: 8501:8501) 및 환경변수(.env) 연동 설정을 포함하세요.

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| 길이 필터를 `videos.list` **직후**에 배치 | 롱폼이 `commentThreads.list` 단계로 넘어가지 않아 쿼터가 몇 배로 줄어듦 |
| `search.list` 는 최후 폴백 | 1회 **100 units** — `@핸들` 은 `channels.list?forHandle`(1 unit)로 먼저 해석 |
| 분석 실패를 수집 실패로 보지 않음 | 댓글 원본은 이미 확보 — `analysis_error` 에 사유만 남기고 대시보드는 계속 동작 |
| 도구 구현과 `@beta_tool` 래퍼 분리 | 대시보드가 `.__wrapped__` 없이 `crawl_channel()` 을 직접 호출 |


### P2 — 2단계: 대시보드 데이터 파일 내려받기 (2026-08-02)

원문:

>**[2단계] 대시보드 데이터 파일 내려받기**
>
>1. 웹 대시보드 UI 기능 확장 데이터 파일 내려받기 기능: 수집된 쇼츠 요약 정보(CSV) 및 댓글/대댓글 원본 전체 데이터(JSON) 다운로드 버튼을 제공하세요.

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| 변환 로직을 [app/exports.py](app/exports.py) 로 분리 | `dashboard.py` 는 렌더링만 담당 — 내보내기 포맷을 UI 밖에서 검증할 수 있음 |
| CSV 는 **UTF-8 BOM** (`utf-8-sig`) | BOM 없이 저장하면 Excel 이 한글 열 이름을 깨뜨림 |
| 표 렌더링과 CSV 가 `shorts_frame()` 하나를 공유 | 화면에 보이는 표 = 내려받는 파일 (열이 어긋날 수 없음) |
| JSON 은 **영상 단위로 그룹핑** | 평평한 스레드 배열보다 후속 분석에서 다루기 쉬움. 쇼츠 목록에 없는 영상의 스레드는 뒤에 붙여 유실 방지 |
| `total_reply_count` 와 `collected_reply_count` 를 **함께** 기록 | 둘이 다르면 API 응답에서 잘린 것 — 받는 쪽이 전량 여부를 판별할 수 있음 |
| JSON 에 LLM 분석 결과는 넣지 않음 | 요청은 "원본 전체 데이터". 분석은 파생물이라 원본 파일을 오염시키지 않음 |
| `st.cache_data` + `채널ID:수집시각` 캐시 키 | 위젯 조작(정렬·필터)마다 rerun 되는 Streamlit 에서 직렬화를 매번 반복하지 않기 위함 |
| 수집 데이터가 없으면 버튼을 **비활성**으로 노출 | 버튼을 숨기면 기능 자체를 못 찾음 — 자리는 두고 사유를 캡션으로 설명 |

내보내는 파일:

| 파일 | 이름 | 내용 |
|---|---|---|
| 쇼츠 요약 | `shorts_{채널}_{수집일}.csv` | 쇼츠 1편 = 1행 · 영상ID/제목/게시일/길이/조회수/좋아요/댓글/좋아요율/댓글율/태그/링크 |
| 댓글 원본 | `comments_{채널}_{수집일}.json` | 영상별 스레드 전량 · 대댓글 포함 · **본문 컷 없음** (R2 의 상한은 프롬프트에만 적용) |

### P3 — [2단계-1] 대시보드 UI/수출 개선 (2026-08-02)

원문:

>**[2단계-1] 웹 대시보드 UI 및 데이터 수출 기능 개선 요청**
>
>1. 대시보드 레이아웃 전체 너비(Full Width) 확장 — `st.set_page_config(layout="wide")` 적용 상태를 재확인하고, 주요 컨테이너 및 차트 요소에 `use_container_width=True` 설정을 빠짐없이 반영
>2. CSV 다운로드 데이터 구성 확장 — 쇼츠 요약 + 해당 쇼츠의 댓글/대댓글 반응 데이터(작성자, 본문, 좋아요, 대댓글 스레드)를 각 행에 텍스트로 묶어 포함. 'CSV 내려받기' 버튼은 전체 표 안에 숨기지 말고 채널명 오른쪽에 배치
>3. 터미널 Ctrl+C 캐시 초기화 모달 방지 — 오용된 `@st.cache_data`/`@st.cache_resource` 를 제거하거나 `st.session_state` 로 대체

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| `layout="wide"` 는 이미 적용 상태 — 실제 원인은 CSS `max-width: 1280px` | 페이지 설정이 아니라 `.block-container` 의 폭 제한이 화면을 묶고 있었음 |
| `use_container_width=True` 대신 `width="stretch"` 사용 | 요청과 **같은 동작**이지만, streamlit 1.49+ 에서 `use_container_width` 는 폐기 예정 인자라 경고가 뜬다 (`requirements.txt` 가 1.49 이상을 요구) |
| 전체 너비로 풀되 카드/본문에는 개별 상한 | 세로(9:16) 썸네일이 화면 폭만큼 커지면 카드 하나가 화면을 삼킴 → `.short-card` 280px, 댓글 본문 90ch |
| 댓글 열은 CSV 에만, 화면 표에는 미표시 | 스레드 전문이 들어간 셀은 표의 행 높이를 무너뜨림. `shorts_frame(with_comments=)` 한 함수로 분기 |
| 스레드를 **좋아요 내림차순**으로 셀에 채움 | 셀이 잘릴 때 남는 것이 공감 많은 댓글이어야 함 |
| 셀 상한 32,000자 + `… 이하 N개 스레드 생략` | Excel 셀 상한은 32,767자 — 넘기면 파일이 깨짐. 전문은 JSON 이 담당 |
| CSV 버튼을 헤더와 내려받기 섹션 **양쪽에** 배치 | 헤더는 즉시 실행용, 하단 섹션은 열 구성·용량 설명용. 위젯 `key` 로 중복 ID 회피 |
| `@st.cache_data` → `st.session_state` 메모 | 캐시 모달의 대상은 함수 캐시뿐이다. 내보내기 페이로드는 세션마다 다르니 전역 캐시일 이유가 없음 |
| `.streamlit/config.toml` 에 `toolbarMode = "minimal"` | 모달의 실제 트리거는 **브라우저에서 누른 `c` 키**다(정적 번들 `handleKeyDown: case 'c' → openClearCacheDialog`). 이 단축키는 toolbarMode 가 DEVELOPER/AUTO(로컬호스트)일 때만 열리므로 minimal 로 막는다 |

### P4 — [2단계-2] UI/UX 다듬기 (2026-08-02)

원문:

>**[2단계-2] 대시보드 UI/UX 및 기능 개선 요청**
>
>1. 단축키 'C' 입력 시 출력되는 캐시 삭제(Clear Cache) 모달 방지 — 불필요한 `@st.cache_data`/`@st.cache_resource` 점검·제거 또는 `st.session_state` 전환, 캐시가 꼭 필요하면 'c' 키 기본 동작을 우회
>2. 채널 기본 정보 메트릭 카드 레이아웃 통일 — KPI 카드의 너비·높이를 시각적으로 동일하게, 세련된 Grid 형태로 정렬
>3. 채널명 옆 상단 CSV 다운로드 버튼 추가 및 **버튼 명칭 사전 확인** — 대체 명칭 후보가 있으면 코드 확정 전에 질문할 것

버튼 명칭은 확정 전에 후보 4개(리포트 다운로드 / 쇼츠·댓글 데이터 / 리포트 받기 / 현행 유지)를
제시해 확인받았고, **"📊 리포트 받기 (CSV)"** 로 결정했습니다. 하단 섹션 버튼은 `⬇ CSV 내려받기`
로 두어 같은 파일이라는 점을 문구로 잇습니다.

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| KPI 를 `st.columns` → **CSS grid 한 장**으로 | 카드마다 별도 블록이면 내용 길이에 따라 높이가 흔들린다. grid 는 `align-items: stretch` 가 기본이라 같은 행의 카드 높이가 자동으로 맞는다 |
| `repeat(auto-fit, minmax(190px, 1fr))` | 남는 폭을 카드가 똑같이 나눠 갖고(너비 균일), 좁은 화면에서는 알아서 줄바꿈된다 |
| 보조 문구가 없어도 `.metric-sub` 자리를 **항상** 렌더 + `margin-top: auto` | '구독자 수' 처럼 보조 문구가 없는 카드만 값 위치가 올라가 기준선이 어긋나던 문제 해소 |
| `.metric-label` 에 `nowrap + ellipsis` | 라벨이 두 줄로 접히면 그 카드만 높아진다 |
| Dockerfile CMD 에도 `--client.toolbarMode=minimal` | config.toml 에 이미 있지만, 작업 디렉터리가 바뀌거나 설정 파일이 볼륨에 가려지면 모달이 되살아난다. CLI 플래그가 설정 파일보다 우선하므로 이중으로 고정 |
| 캐시 데코레이터는 **0개** 유지 | 모달의 삭제 대상은 함수 캐시뿐이다. 내보내기 페이로드는 세션마다 다르므로 `st.session_state` 로 충분하고, 캐시가 없으면 모달이 떠도 잃을 것이 없다 |

### P5 — [3단계] 채널 종합 분석 확장 & HTML 파일 분리 (2026-08-03)

원문:

>**[3단계] 대시보드 LLM 채널 종합 분석 확장 및 HTML 파일 분리 refactoring**
>
>아래 2가지 개선 및 리팩토링 요구사항을 프로젝트 코드에 반영해 주세요.
>
>1. 크롤링 채널 전체 대상 LLM(Claude) 종합 분석 리포트 기능 추가
>   - 기존의 단일 댓글/대댓글 반응 요약 수준을 넘어, 수집된 전체 쇼츠 메타데이터(조회수, 좋아요, 영상 길이, 게시 빈도)와 시청자 댓글 반응을 통합 분석하는 '채널 전반 분석(Channel Overall Analysis)' 로직을 추가하세요.
>   - LLM 프롬프트를 확장하여 (1) 채널 핵심 성과 요약, (2) 인기 쇼츠 성공 요인, (3) 시청자 반응 트렌드, (4) 향후 콘텐츠 전략 제안이 포함된 종합 리포트를 생성하도록 구현하고, 대시보드 상단/중간에 시각적으로 강조된 영역으로 표출하세요.
>
>2. Python 파일 내 내장 HTML/CSS 마크업 분리 (web/index.html / styles.css)
>   - 현재 app.py 등 파이썬 코드 내부에 st.markdown() 형태로 하드코딩되어 있는 inline HTML 및 CSS 스타일 구문을 전면 리팩토링하세요.
>   - HTML 템플릿 및 CSS 스타일 파일을 별도 디렉토리(예: templates/ 또는 web/index.html, web/styles.css)로 분리해 관리하세요.
>   - 파이썬 코드에서는 외부 HTML/CSS 파일을 읽어와(read) Jinja2 또는 Python string formatting을 통해 데이터만 바인딩하여 렌더링하도록 깔끔하게 구조화하세요.

주요 설계 판단 — 1) 채널 종합 분석:

| 판단 | 이유 |
|---|---|
| 기존 R1 을 고치지 않고 **2단계 분석**으로 분리 (`analyze_comments` → `analyze_channel`) | 보는 대상이 다르다. R1 은 댓글만, R6 은 지표+반응. 한 프롬프트에 합치면 어느 쪽도 깊어지지 않고, 댓글이 없는 채널에서 통째로 실패한다 |
| 2단계가 1단계 **산출물을 입력으로** 받되 필수는 아님 | 댓글 분석이 실패해도 지표만으로 리포트가 나온다. `crawl_channel` 이 5단계 실패와 무관하게 6단계를 실행 |
| 원지표를 그대로 던지지 않고 **미리 집계해서** 프롬프트에 넣음 | 길이 구간별 평균·월별 게시 편수는 모델이 토큰을 태워 계산할 일이 아니다. 파이썬이 계산해 주면 모델은 해석에만 집중한다 |
| 게시 빈도를 `CrawlResult` 파생 속성으로 (`upload_interval_days` 등) | 프롬프트·KPI 카드·에이전트 JSON 세 곳이 같은 값을 쓴다. 계산이 흩어지면 화면과 리포트의 숫자가 어긋난다 |
| 성공 요인·트렌드·전략을 **평평한 문자열이 아니라 중첩 스키마**로 | 근거(`evidence`)와 우선순위(`priority`)를 분리해야 카드에서 배지·본문으로 나눠 그릴 수 있다. 문장 하나로 받으면 UI 가 파싱해야 한다 |
| `overall` / `overall_error` 를 별도 필드로 추가 | 기존 저장 JSON 에 없는 필드라 기본값 `None` — 예전 수집 결과도 그대로 열린다 |
| 대시보드 배치는 KPI **바로 아래** | "상단/중간에 강조" 요구. 지표를 본 직후 해석을 읽는 순서가 자연스럽고, 좌측 액센트+그림자로 다른 카드보다 한 단계 높은 표면을 준다 |

주요 설계 판단 — 2) 마크업 분리:

| 판단 | 이유 |
|---|---|
| Jinja2 **매크로 라이브러리** 형태의 `web/index.html` | Streamlit 은 단일 문서를 렌더하지 않고 위젯 사이에 조각을 끼워 넣는다. `<html>` 뼈대가 아니라 호출 가능한 조각 모음이어야 맞다 |
| CSS 에 색 리터럴을 두지 않고 `var(--토큰)` 참조만 | 색의 단일 진실 공급원은 `theme.py`. 토큰을 `:root` 로 주입하므로(`templates.stylesheet()`) 팔레트 교체 시 CSS 를 건드릴 필요가 없다 |
| Python string formatting 대신 **Jinja2** 채택 | CSS/HTML 은 중괄호 천지라 `.format()` 을 쓰면 전부 이스케이프해야 한다. 게다가 Jinja 의 autoescape 가 기존 `html.escape` 헬퍼를 대체한다 |
| `templates.render()` 가 렌더 결과를 **flatten** | `st.markdown` 은 HTML 을 마크다운으로 먼저 파싱한다. 빈 줄이 HTML 블록을 끊고 뒤이은 들여쓴 줄이 코드 블록이 되어 태그가 화면에 그대로 찍힌다 |
| LLM 이 쓴 필드에 `plain` 필터 | 모델이 `<b>`, `**굵게**` 를 뱉으면 카드에 기호로 남는다. 프롬프트(평문 지시) + 필터 + flatten 3중 방어 |
| `Dockerfile` 에 `COPY web ./web` 추가 | 런타임에 읽는 파일이므로 빠지면 기동 즉시 실패한다 |

> ⚠️ **운영 주의:** `web/` 은 Jinja 가 mtime 을 보고 매 렌더마다 다시 읽지만, `app/` 의 파이썬 모듈은
> 이미 `sys.modules` 에 올라가 있어 **재시작 전까지 반영되지 않습니다**(Windows→Linux 바인드 마운트는
> 파일 감시가 뜨지 않음). 파이썬을 고쳤으면 `docker compose restart shorts-agent`.

### P6 — [4단계] docker-compose 분리 (2026-08-04)

원문:

>**[4단계] 로컬/운영 docker-compose 분리 및 EC2 배포 구성**
>
>지금까지는 `docker-compose.yml` 하나로 로컬에서만 띄웠습니다. 이 앱을 AWS EC2 에 올려
>도메인(`shorts-agent.duckdns.org`)으로 접속할 수 있게 하려고 합니다. 아래대로 구성해 주세요.
>
>1. **compose 파일 분리**
>   - `docker-compose.yml` — 로컬/공용 정의. 앱 컨테이너(`shorts-agent`) 빌드·포트·볼륨·env_file 을 담당하며 이 파일만으로 로컬 개발이 그대로 돌아가야 합니다.
>   - `docker-compose.prod.yml` — EC2 전용 **오버레이**. 앱 서비스를 다시 정의하지 말고, 운영에만 필요한 리버스 프록시(`nginx`)와 인증서 발급기(`certbot`)만 추가하세요.
>   - 운영 기동은 두 파일을 겹쳐 올리는 방식(`-f docker-compose.yml -f docker-compose.prod.yml`)을 전제로 합니다.
>2. **nginx 리버스 프록시 (`nginx.conf`)**
>   - 80 포트는 Let's Encrypt 챌린지 경로만 열고 나머지는 443 으로 301 리다이렉트.
>   - 443 에서 TLS 종료 후 `shorts-agent:8501` 로 프록시.
>   - Streamlit 은 WebSocket 으로 통신하므로 `Upgrade`/`Connection` 헤더를 반드시 넘기세요.
>   - `.env`, `.git` 같은 숨김 경로(`/\.`)는 차단하세요.
>3. **인증서** — certbot 컨테이너와 nginx 가 `./certbot/conf`(인증서), `./certbot/www`(웹루트 챌린지) 두 디렉터리를 공유하도록 볼륨을 맞추세요.

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| prod 를 **독립 파일이 아니라 오버레이**로 (`shorts-agent` 재정의 없음) | 앱 정의를 양쪽에 복제하면 빌드 컨텍스트·볼륨·env_file 이 서서히 갈라진다. 운영에서만 늘어나는 것은 프록시 계층뿐이므로 그것만 얹는다 |
| nginx 를 **컨테이너로** (EC2 호스트 설치 아님) | `shorts-agent` 를 compose 네트워크의 서비스명으로 부를 수 있다. 호스트 nginx 였다면 컨테이너 포트를 호스트에 노출해야 하고, 그 포트가 곧 HTTPS 우회 경로가 된다 |
| `proxy_pass` 대상이 `localhost` 가 아니라 **`shorts-agent:8501`** | compose 기본 네트워크의 DNS 가 서비스명을 컨테이너 IP 로 풀어 준다. IP 를 박으면 재기동 때마다 바뀐다 |
| `proxy_http_version 1.1` + `Upgrade`/`Connection "upgrade"` | Streamlit 은 최초 HTML 만 HTTP 이고 이후 상태 갱신은 전부 WebSocket 이다. 이 세 줄이 빠지면 화면은 뜨는데 위젯이 전혀 반응하지 않는다 (기본값 HTTP/1.0 은 업그레이드 자체가 불가) |
| `X-Forwarded-Proto $scheme` 전달 | 프록시 뒤의 앱은 자기가 HTTP 로 서비스된다고 착각한다. 리다이렉트·절대 URL 생성이 `http://` 로 나가 혼합 콘텐츠가 되는 것을 막는다 |
| 80 을 **닫지 않고** ACME 챌린지 경로만 남김 | Let's Encrypt 의 HTTP-01 검증은 평문 80 으로 온다. 전부 리다이렉트하면 갱신이 실패한다 — `location /.well-known/acme-challenge/` 를 리다이렉트 **앞에** 둔 이유 |
| 인증서를 이미지가 아니라 **호스트 볼륨**(`./certbot/conf`)에 | 인증서는 90일마다 갱신된다. 이미지에 구우면 갱신 때마다 재빌드해야 하고, certbot 컨테이너와 공유할 수도 없다 |
| `location ~ /\. { deny all; }` | 소스 디렉터리를 그대로 마운트하는 구성이라 `.env`·`.git` 이 파일 시스템상 같은 트리에 있다. 정적 서빙 경로가 아니더라도 차단 규칙을 먼저 세워 둔다 |
| nginx 만 `restart: always` (앱은 `unless-stopped`) | 진입점은 인스턴스 재부팅 후 무조건 살아나야 한다. 반대로 앱은 운영자가 의도적으로 내린 상태를 재부팅이 뒤집지 않는 편이 낫다 |

> ⚠️ **오버레이 병합 시 남는 항목** — `docker-compose.yml` 의 `ports: "8501:8501"` 과 개발용
> 소스 바인드 마운트(`./app`, `./web`, `./streamlit_app.py`)는 병합 결과에도 그대로 살아 있습니다.
> 즉 EC2 보안 그룹에서 8501 이 열려 있으면 `http://<EC2-IP>:8501` 로 **nginx 와 TLS 를 우회**할 수
> 있습니다. 보안 그룹에서 80/443 만 허용하거나, prod 오버레이에서 `ports: []` 로 덮어쓰세요.

### P7 — [5단계] YouTube API 키를 사용자 입력으로 전환 (2026-08-04)

원문:

>**[5단계] YouTube Data API v3 키를 사용자별 입력 방식으로 전환**
>
>YouTube API 키를 `.env` 에 고정하지 않고, 유저별로 사이트에서 자기 키를 추가해 결과 리포트를
>확인할 수 있게 바꾸고 싶습니다. 사이트에서 개별적으로 입력하는 것이 보안상 문제가 없는지 확인하고,
>보안상 문제 없는 방법을 제시한 뒤 확인을 받고 작업을 진행해 주세요.

검토 결과 **키 자체보다 서버가 키를 흘리는 경로가 위험**하다는 결론이었고, 세 가지를 확인받아 진행했습니다
— (1) 키는 **세션 메모리에만** 보관(영구 저장·브라우저 저장 안 함), (2) 수집 결과는 **세션별 격리**,
(3) Claude 키는 이번 범위에서 제외하고 `.env` 유지.

주요 설계 판단:

| 판단 | 이유 |
|---|---|
| 키를 `st.session_state` 에만 보관 | 서버가 장기 보관하는 비밀이 0개가 된다. 암호화 영구 저장은 복호화 마스터키를 서버가 들고 있어야 해서, 볼륨이 털리면 **전원의 키**가 한 번에 나간다 — 저장하지 않는 편이 근본적으로 안전하다 |
| **`security.mask()` 도입이 이번 작업의 핵심** | `googleapiclient` 는 키를 URL 쿼리스트링(`?key=...`)에 싣는다. 따라서 `HttpError` 문자열에 키 원문이 들어가고, 기존 `st.error(f"수집 실패: {exc}")` 는 그것을 **화면에 그대로 출력**하고 있었다. 사용자 키를 받는 순간 이 경로가 곧 유출 경로가 된다 |
| 로깅은 Filter 가 아니라 **Formatter** 로 마스킹 | 트레이스백은 포맷 시점에야 문자열이 된다. Filter 단계에서는 `record.exc_info` 안의 키를 지울 수 없다 |
| 마스킹 패턴에 `AIza…`·`sk-ant-…` **키 형식 자체**를 포함 | `?key=` 형태만 막으면 JSON 본문·설정 덤프처럼 URL 밖으로 나가는 경로가 남는다 |
| 수집 결과를 `sessions/<난수>/` 로 격리 | 볼륨 하나를 공유하는 구조라, 격리 전에는 **누가 수집하든 사이드바 "저장된 채널" 에 전부 노출**됐다. 남의 키로 뽑은 리포트가 다른 방문자에게 보이던 문제 |
| 인메모리 캐시(`_MEMORY`) 키에도 네임스페이스 포함 | 디스크만 나누면 한 프로세스 안에서 캐시를 통해 그대로 새어 나간다 |
| `store.purge_expired()` (기본 7일) | 세션이 끝나면 난수 네임스페이스를 아는 주체가 없어 주인도 못 찾는다. 정리하지 않으면 볼륨에 영원히 쌓인다 |
| `crawl_channel(channel, api_key, namespace, …)` — 키를 **필수 인자**로 | 설정으로 폴백하면 "웹=사용자 키 / CLI=환경변수" 구분이 조용히 무너진다. 시그니처가 강제하면 새 호출부가 생겨도 실수할 수 없다 |
| `settings.youtube_api_key` 는 **CLI 에이전트 전용**으로 남김 | `python -m app.agent` 는 운영자가 직접 돌리는 경로라 환경변수가 맞다. 대신 대시보드가 이 값을 읽지 않게 해, 공개 서버에서 방문자가 운영자 쿼터를 쓰는 일을 원천 차단 |
| 리퍼러 제한이 아니라 **IP 제한**을 안내 | 브라우저가 아니라 서버가 대신 호출하므로 HTTP 리퍼러 제한을 걸면 **정상 요청이 차단**된다. 실제로 겪기 쉬운 함정이라 안내 문구와 오류 메시지 양쪽에 넣었다 |

> ℹ️ **정확히 말하면** — 키는 `type="password"` 입력값이므로 해당 사용자의 브라우저와 서버 사이
> TLS WebSocket 을 오갑니다(다른 사용자에게는 가지 않습니다). 서버 쪽에서 디스크·로그에 남기지
> 않을 뿐이며, 따라서 **HTTPS 없이 운영하면 이 설계의 전제가 깨집니다**(4단계 nginx 구성이 전제).
>
> ⚠️ **남은 범위** — 이번에 바꾸지 않은 `ANTHROPIC_API_KEY` 는 여전히 서버 `.env` 값이라,
> 공개 도메인에서는 방문자 누구나 **운영자 Claude 크레딧**을 소모시킬 수 있습니다(P7-1 감사 참조).
>
> ⚠️ **호환성** — 격리 이전에 `/data` 최상단에 저장된 `crawl_*.json` 은 어느 세션에도 속하지 않아
> 목록에 뜨지 않습니다(파일은 지워지지 않습니다).

### P7-1 — `ANTHROPIC_API_KEY` 노출 경로 감사 (2026-08-04)

원문:

>추가적으로 ANTHROPIC_API_KEY는 노출되는 구멍이 없는지 확인하고 검증하세요.

**전제 확인 — Anthropic SDK 는 키를 예외에 싣지 않습니다.** 실제 호출로 검증한 결과
`AuthenticationError`·`APIConnectionError`·`BadRequestError` 의 `str()`/`repr()`/트레이스백,
그리고 `ANTHROPIC_LOG=debug` 로그 어디에도 키가 없었습니다. `x-api-key` **헤더**로 보내기 때문이며,
키를 URL 쿼리스트링에 싣는 `googleapiclient` 와 구조가 다릅니다 — 위험도가 같지 않습니다.

그래서 발견된 구멍은 SDK 가 아니라 **우리 코드가 예외를 다루는 방식**에 있었습니다.

| 발견 | 실태 | 조치 |
|---|---|---|
| **CLI 진입점에 마스킹 미설치** (실증된 누출) | `app/agent.py` 가 `logging.basicConfig` 만 부르고 마스킹을 걸지 않아, `python -m app.agent` 경로의 트레이스백에 **YouTube 키가 원문 그대로** 남았다. 마스킹을 대시보드에만 설치한 것이 원인 | `security.configure_logging()` 으로 basicConfig+마스킹을 한 함수에 묶고 두 진입점이 모두 호출 |
| `analysis_error` / `overall_error` 무마스킹 | 이 문자열은 `/data` JSON 에 **저장되고**, 화면에 표시되고, `summarize_for_model()` 을 통해 **모델에게도 전달**된다. 세 경로 모두 되돌릴 수 없다 | 대입 시점에 `security.mask(exc)` 적용 |
| Streamlit `showErrorDetails = "full"` (기본값) | 잡히지 않은 예외의 **원문 메시지와 트레이스백을 브라우저에 그대로 렌더**한다. 마스킹은 로그와 우리가 잡은 예외만 덮으므로 이 경로는 코드로 막을 수 없다 | 운영 오버레이에만 `STREAMLIT_CLIENT_SHOW_ERROR_DETAILS=none`. 로컬은 기본값 유지 |

이상 없음으로 확인된 항목: `.env` 는 git 이력에 없고(`.gitignore`), 이미지에도 들어가지 않으며
(`.dockerignore`), 컨테이너 환경변수로만 존재합니다. `st.secrets` 미사용, 정적 파일 서빙 미활성.

| 판단 | 이유 |
|---|---|
| 로깅 설정을 `configure_logging()` **한 함수로 강제** | 진입점마다 `basicConfig` 를 따로 부르면 한쪽에서 마스킹이 빠진다 — 실제로 그렇게 빠졌다. 설정과 방어를 분리 가능한 형태로 두지 않는다 |
| SDK 가 안전해도 `analysis_error` 를 마스킹 | 지금 안전한 것은 *현재 SDK 버전의 동작*일 뿐이다. 디스크·화면·모델 세 곳으로 동시에 나가는 값은 발생 시점에 한 번 막는 편이 싸다 |
| `showErrorDetails` 를 config.toml 이 아니라 **prod 오버레이**에 | 로컬에서 트레이스백이 사라지면 개발이 느려진다. 노출이 문제되는 곳은 공개 서버뿐이므로 4단계에서 만든 경계를 그대로 쓴다 |
| `ports: []` 로 8501 을 닫지 **않음** | compose 는 `ports` 같은 시퀀스를 오버레이에서 **이어 붙이므로**(대체가 아님) 빈 리스트로 지울 수 없다. 이 경로는 EC2 보안 그룹에서 막아야 한다 |

### P8 — [6단계] 구조화 · LLM 신뢰성 · 루프 가드 (2026-08-06)

원문:

>**[6단계] 에이전트 신뢰성 강화 및 프로젝트 구조화**
>
>기존 유튜브 쇼츠 크롤링 에이전트의 코드 구조를 모듈화하여 정돈하고, LLM 호출 신뢰성, 루프 가드 작업을 진행하세요.
>
>1. **프로젝트 디렉터리 구조화 (Refactoring)** — 기존 `app/` 디렉터리 하위에 펼쳐져 있던 파일들을 기능별 패키지 모듈로 분리하여 재구성하세요.
>2. **LLM 호출 신뢰성 & 토큰/비용 로깅** — 모든 LLM 호출은 LiteLLM 을 경유하도록 구현하세요. Claude API 키를 기본 사용하며, API 일시적 오류 및 Rate Limit 발생 시 지수 백오프(Exponential Backoff) 기반 자동 재시도를 적용하세요. (단, 폴백 체인은 구성하지 않음)
>3. **에이전트 루프 가드 (Loop Guard)** — 도구 호출 최대 반복 횟수를 제한하세요 (예: `max_iterations = 5`). 도구 실행 실패 시 예외를 던지지 않고 에러 내용을 LLM 에게 피드백으로 반환하여, 모델이 다른 인자나 대체 도구로 재시도할 수 있도록 처리하세요.

패키지 경계는 **의존 방향이 한쪽으로만 흐르도록** 잘랐습니다 —
`core → domain → llm → collector → analysis → agent / dashboard`.

주요 설계 판단 — 1) 구조화:

| 판단 | 이유 |
|---|---|
| 파일 이름이 아니라 **의존 방향**으로 자름 | "기능별"을 이름으로만 나누면 순환 임포트가 남는다. `core` 는 아무도 임포트하지 않고 `dashboard`/`agent` 는 아무도 임포트하지 않는다는 규칙이 경계를 유지시킨다 |
| `schemas.py` 를 `domain/models.py` + `domain/analysis.py` 로 분리 | 한쪽은 YouTube 에서 받는 데이터, 다른 쪽은 **모델에게 주는 지시문**(R3·R8)이다. 수명도 리뷰 대상도 다르다 |
| `analyzer.py` 를 prompts / payloads / comments / channel 로 | 프롬프트 전문과 집계 로직과 호출부가 한 파일에 있으면, 프롬프트를 고칠 때 diff 가 코드에 묻힌다 |
| `tools.py` 의 수집 구현을 `collector/crawler.py` 로 이동 | 대시보드는 도구 스키마를 거치지 않고 수집만 부른다. 도구 래퍼와 파이프라인을 한 파일에 두면 UI 가 에이전트 계층에 의존하게 된다 |
| `dashboard.py`(734줄)를 state/sidebar/sections/charts/widgets 로 | 렌더 순서(app.py)와 섹션 구현을 갈라 두면, 화면 하나를 고칠 때 읽을 파일이 하나로 좁혀진다 |
| 이동은 전부 `git mv` | 파일 이력이 끊기면 "이 줄이 왜 이렇게 됐나"를 P1~P7 로 되짚을 수 없다 |

주요 설계 판단 — 2) LLM 신뢰성:

| 판단 | 이유 |
|---|---|
| `litellm` 임포트를 **gateway·retry 두 파일로 제한** | 호출부가 늘어나도 재시도·토큰 집계·비용 로깅·마스킹 중 하나가 빠질 수 없다. "모든 호출이 LiteLLM 을 경유"는 규칙이 아니라 **구조**여야 강제된다 |
| `num_retries=0` 으로 LiteLLM 내부 재시도를 끔 | 우리 백오프와 겹치면 실제 대기가 곱해지고, 로그의 `attempts` 가 실제 호출 수와 어긋난다 |
| 재시도 대상을 **화이트리스트**로 (429·타임아웃·연결·5xx) | 정체 모를 예외까지 재시도하면 우리 쪽 버그가 조용히 5배 요금이 된다. 인증·잘못된 요청은 같은 요청을 다시 보내도 같은 실패다 |
| `retry-after` 헤더가 계산된 백오프를 **덮어씀** | 서버는 실제 해제 시각을 알고 우리는 모른다. 헤더를 무시하면 한도가 풀리기 전에 다시 두드려 한도를 더 밀어낸다 |
| 지연에 25% 흔들림 | 여러 호출이 같은 초에 몰려 재시도하는 것을 흩는다. 비율을 25% 로 묶어 두어 지연은 **여전히 단조 증가**한다 |
| 폴백 체인 없음 (요구사항) | 모델을 바꿔 재시도하면 같은 요청이 서로 다른 모델의 답으로 섞여, 리포트에 남은 숫자가 어느 모델 것인지 사후에 알 수 없다 |
| 단가표를 **직접 들고** LiteLLM 것을 쓰지 않음 | LiteLLM 은 모르는 모델에 비용 0 을 돌려주기도 한다. 리포트에 남는 숫자이므로 **모르면 0 이 아니라 `null`** 이어야 한다 |
| 사용량 로그를 **JSON 한 줄**로, 실패한 호출도 기록 | 그대로 `jq` 로 집계된다. 실패는 비용이 0이어도 원인과 시도 횟수가 남아야 한다 |
| 로그 페이로드에 `security.mask()` 를 **한 번 더** | 예외 문자열에 키가 실려 오는 경로가 있고, 이 줄은 파일로 남아 되돌릴 수 없다 (P7-1 과 같은 판단) |
| 구조화 출력은 본문 → **도구 호출 인자** 순으로 회수 | LiteLLM 은 공급자에 따라 `response_format` 을 도구 호출로 번역한다. 본문만 보면 그 경로에서 빈손이 된다 |

주요 설계 판단 — 3) 루프 가드:

| 판단 | 이유 |
|---|---|
| 상한 도달 시 **도구를 떼고 한 번 더** 호출 | 그냥 끊으면 사용자는 빈손이다. 도구가 없으면 모델은 반드시 텍스트로 답하므로, 그때까지 모은 것으로 결론을 만들게 한다 (R4-1) |
| 도구 실패를 `dispatch()` 한 곳에서 흡수 | 도구마다 try/except 를 두면 새 도구에서 빠진다. **어떤 구현이 무엇을 던지든** 모델에게는 오류 페이로드로만 도착한다 |
| `TypeError` 를 잡아 파이썬 메시지를 그대로 전달 | "unexpected keyword argument 'foo'" 는 이미 모델이 읽고 고칠 수 있는 최선의 피드백이다. 따로 검증기를 쓸 이유가 없다 |
| **LLM 호출 실패는 흡수하지 않음** | 키 누락·재시도 소진은 모델이 인자를 바꿔 고칠 수 있는 문제가 아니다. 피드백으로 되돌리면 상한까지 헛돈다 → `status="error"` 로 즉시 종료 |
| 예외 대신 `AgentResult` 반환 | 중단된 실행에서도 대화 히스토리·누적 토큰·비용이 남는다. 실패한 실행일수록 그 기록이 필요하다 |
| 도구 호출 왕복을 세고 **도구 호출 건수는 따로** 기록 | 한 턴이 도구를 여러 개 부르므로 두 수가 다르다. 상한은 왕복 기준이고, 비용 감각은 건수 쪽에 있다 |
| 시스템 프롬프트에 "error 를 읽고 고쳐 재시도" 지시 추가 | 가드는 실패를 **되돌려 줄** 뿐이다. 읽고 고치라고 말하지 않으면 같은 인자로 상한까지 반복한다 |

> ⚠️ **`ANTHROPIC_API_KEY` 범위는 그대로입니다** — LiteLLM 을 거쳐도 Claude 키는 여전히 서버
> `.env` 값이라, 공개 도메인에서는 방문자 누구나 운영자 크레딧을 소모시킬 수 있습니다(P7-1 참조).
> 다만 이제 소모량이 `app.llm.usage` 로그에 호출 단위로 남아 **사후 추적은 가능**합니다.

---

## 2. 런타임 프롬프트 🤖

### R1 — 댓글 반응 분석 시스템 프롬프트

**위치:** [app/analysis/prompts.py](app/analysis/prompts.py) `COMMENT_SYSTEM_PROMPT` ·
**호출:** [app/analysis/comments.py](app/analysis/comments.py) → `gateway.complete_structured(...)` (LiteLLM 경유 구조화 출력)
**모델:** `LLM_PROVIDER/CLAUDE_MODEL` (기본 `anthropic/claude-opus-5`) · **max_tokens:** `LLM_MAX_TOKENS` (기본 16000)

```text
당신은 유튜브 쇼츠 채널의 시청자 반응을 분석하는 콘텐츠 전략 애널리스트입니다.

주어진 것은 한 채널의 쇼츠 영상별 성과 지표와, 그 영상들에 달린 댓글 및 대댓글입니다.
다음 원칙에 따라 분석하세요.

- 근거 기반: 실제 댓글에 나타난 표현만 인용하고, 데이터에 없는 내용을 지어내지 마세요.
- 대댓글도 하나의 신호입니다. 논쟁이 붙은 스레드는 반응의 온도를 보여줍니다.
- 좋아요가 많은 댓글은 다수의 공감을 대변하므로 가중치를 두세요.
- 감정 비율(positive/negative/neutral)의 합은 정확히 100이어야 합니다.
- 키워드는 단순 빈출 단어가 아니라 '반복되는 반응 패턴'을 잡아내세요.
  (예: "편집 속도가 빠르다", "다음 편 요청", "자막 가독성 불만")
- 제안(content_recommendations)은 채널 운영자가 다음 촬영에서 바로 실행할 수 있는 형태로 쓰세요.
- 모든 출력은 한국어로 작성하세요.
- 모든 필드는 서식 없는 평문으로 쓰세요. 마크다운(**, #, -)이나 HTML 태그를 넣지 마세요.
```

각 지시의 의도:

| 지시 | 왜 넣었는가 |
|---|---|
| 근거 기반 / 지어내지 말 것 | 대시보드가 이 결과를 **채널 운영 판단 근거**로 보여줌 — 환각은 곧 잘못된 기획으로 이어짐 |
| 대댓글도 신호 | 대댓글까지 수집한 이 크롤러의 차별점을 분석 단계에서도 살리기 위함 |
| 좋아요 가중치 | 댓글 1건 = 1표가 아님. 공감 수가 다수 의견의 크기를 나타냄 |
| 합계 정확히 100 | 감정 분포를 **100% 폭 누적 막대**로 그리므로 합이 어긋나면 차트가 왜곡됨 |
| 빈출 단어 아닌 '반응 패턴' | "ㅋㅋㅋ", "대박" 같은 무의미 토큰이 키워드 칩을 채우는 것을 막음 |
| 실행 가능한 제안 | 대시보드 '다음 쇼츠 기획 제안' 블록이 그대로 액션 아이템이 되도록 |
| 한국어 출력 | UI 전체가 한국어 |
| 서식 없는 평문 (P5) | 결과가 HTML 카드 안에 그대로 들어간다. `**굵게**` 나 `<b>` 가 오면 화면에 기호로 남음 |

### R2 — 분석 사용자 메시지 & 페이로드 구조

**위치:** [app/analysis/prompts.py](app/analysis/prompts.py) `COMMENT_INSTRUCTION` /
[app/analysis/payloads.py](app/analysis/payloads.py) `build_analysis_payload`

```text
다음은 한 쇼츠 채널에서 수집한 영상별 댓글과 대댓글입니다.
시청자 반응을 요약하고, 감정 분포와 반복 키워드, 요구사항을 추출해 주세요.

{payload}
```

`{payload}` 는 영상 단위로 묶은 마크다운 블록입니다:

```text
# 채널: {채널명}

## 영상 [{video_id}] {제목}
- 길이 {n}초 / 조회수 {n} / 좋아요 {n} / 댓글 {n}
- 댓글(좋아요 {n}): {본문}
  └ 대댓글(좋아요 {n}): {본문}
```

컨텍스트·비용 보호 상한 (`payloads.py` 상수):

| 상수 | 값 | 의도 |
|---|---|---|
| `MAX_THREADS_PER_VIDEO` | 25 | **좋아요 내림차순 정렬 후** 상위만 — 공감 많은 댓글이 잘리지 않음 |
| `MAX_REPLIES_PER_THREAD` | 5 | 긴 논쟁 스레드 하나가 컨텍스트를 독차지하지 않게 |
| `MAX_COMMENT_CHARS` | 400 (대댓글 250) | 장문 댓글 컷 |

> 이 상한은 **프롬프트에만** 적용됩니다. 대시보드의 댓글 원본 JSON 내려받기는 수집한 전량을 담습니다.

### R3 — 구조화 출력 스키마 (스키마도 프롬프트다)

**위치:** [app/domain/analysis.py](app/domain/analysis.py) `CommentAnalysis` 및 하위 모델

`response_format=CommentAnalysis` 로 넘기므로 각 필드의 `Field(description=...)` 이 모델에게는
**필드별 지시문**으로 작동합니다. 출력 형태를 바꾸려면 시스템 프롬프트가 아니라 여기를 고치세요.

| 필드 | description (= 지시문) |
|---|---|
| `overall_summary` | 채널 전체 시청자 반응 총평 (3-5문장) |
| `sentiment.positive/negative/neutral_pct` | 각 비율(0-100 정수) |
| `sentiment.rationale` | 이 비율로 판단한 근거 요약 |
| `top_keywords[].keyword` | 댓글에서 반복적으로 등장한 키워드 또는 표현 |
| `top_keywords[].mention_count` | 해당 키워드가 등장한 대략적인 댓글 수 |
| `top_keywords[].sentiment` | 이 키워드가 등장하는 맥락의 지배적 감정 |
| `top_keywords[].example` | 대표 댓글 원문 발췌 (한 문장) |
| `viewer_requests` / `praise_points` / `complaint_points` | 반복된 요청 / 칭찬 / 불만·개선점 |
| `content_recommendations` | 다음 쇼츠 기획을 위한 실행 가능한 제안 |
| `per_video[]` | 영상별 반응 2-3문장 요약 + 지배 감정 |

거부(`content_filter`)와 파싱 실패는 `LLMSchemaError` 로 올려 `analysis_error` 에 남기고,
수집 데이터 자체는 그대로 보존합니다.

### R4 — 에이전트 시스템 프롬프트

**위치:** [app/agent/prompts.py](app/agent/prompts.py) `SYSTEM_PROMPT` ·
**호출:** [app/agent/loop.py](app/agent/loop.py) `run_agent()` — 직접 구현한 루프 (P8)

```text
당신은 유튜브 쇼츠 채널 분석 에이전트입니다.

사용 가능한 도구:
- youtube_channel_crawler: 채널의 쇼츠(60초 이하)와 댓글/대댓글을 수집하고 반응을 분석합니다.
- get_crawling_results: 이미 수집한 결과를 다시 읽습니다.

작업 원칙:
- 사용자가 채널을 지정하면 먼저 youtube_channel_crawler 로 수집하세요.
  같은 채널을 이미 수집했다면 재수집 대신 get_crawling_results 를 쓰세요(쿼터 절약).
- 롱폼은 분석 대상이 아닙니다. 쇼츠 성과와 시청자 반응에만 집중하세요.
- 도구가 반환한 수치만 인용하고, 없는 데이터를 추정해서 말하지 마세요.
- 도구가 error 필드를 돌려주면 그 내용을 읽고 인자를 고쳐 다시 시도하거나 다른 도구를 쓰세요.
  같은 인자로 같은 도구를 반복 호출하지 마세요.
- 최종 답변은 한국어로, 결론을 먼저 쓰고 근거를 뒤에 붙이세요.
```

의도: **재수집 억제**가 핵심입니다. 크롤링 1회는 수십 units 를 소모하므로,
저장된 결과가 있으면 읽기 도구로 유도합니다.
오류 대응 지시(P8)는 루프 가드와 짝입니다 — 도구 실패가 예외 대신 피드백으로 돌아오므로,
그 피드백을 **읽고 고치라**고 명시해야 같은 인자로 상한까지 헛돌지 않습니다.

### R4-1 — 루프 가드 최종 턴 지시 (P8 신규)

**위치:** [app/agent/prompts.py](app/agent/prompts.py) `FINAL_TURN_INSTRUCTION` ·
**호출:** 반복 상한 도달 시 도구를 떼고 한 번 더 호출할 때 user 메시지로 주입

```text
도구 호출 한도에 도달했습니다. 더 이상 도구를 쓸 수 없습니다.
지금까지 도구가 돌려준 내용만으로 최종 답변을 작성하세요.
확보하지 못한 정보가 있으면 추정하지 말고 무엇이 비었는지 밝히세요.
```

의도: 상한에 걸렸을 때 **빈손으로 끝내지 않기** 위함입니다. 도구를 뗀 채 한 번 더 부르면
모델은 반드시 텍스트로 답하고, "추정하지 말고 빈 곳을 밝히라"는 지시가 부족한 근거를
그럴듯하게 메우는 것을 막습니다.

### R5 — 도구 설명 (description = 도구 프롬프트)

**위치:** [app/agent/tools.py](app/agent/tools.py) `TOOL_SPECS` — LiteLLM 표준(OpenAI 호환)
함수 스키마의 `description` 이 그대로 모델에게 전달되므로, **description 수정은 프롬프트 수정입니다.**
(P8 이전에는 `@beta_tool` 이 docstring 을 스키마로 변환했고, 문장은 그대로 옮겼습니다.)

| 도구 | 모델에게 전달되는 핵심 문장 |
|---|---|
| `youtube_channel_crawler` | "롱폼(60초 초과) 영상은 분석 대상에서 완전히 제외되어 API 쿼터를 절약한다. 수집 결과는 저장되므로 이후 `get_crawling_results` 로 다시 읽을 수 있다." · `include_analysis`: "댓글 감정/키워드 분석과 **채널 전반 종합 분석**(성과 요약·성공 요인·반응 트렌드·콘텐츠 전략)까지 수행한다" (P5) |
| `get_crawling_results` | "채널 메타데이터, 쇼츠 성과 순위, 시청자 댓글/대댓글 분석, 그리고 **채널 전반 종합 분석**(`channel_overall_analysis`)을 포함한다. `channel_id` 를 비워두면 가장 최근에 수집한 채널을 반환한다." (P5) |

두 도구 모두 **예외를 던지지 않고** `{"error": ...}` JSON 을 반환합니다 — 모델이 실패를 읽고
스스로 다음 행동을 고를 수 있게 하기 위함입니다. `get_crawling_results` 는 실패 시
`available_channels` 를 함께 돌려주어 모델이 올바른 `channel_id` 로 재시도하도록 유도합니다.

P8 에서 이 원칙이 `dispatch()` 로 올라가, **도구 구현이 무엇을 던지든** 모델에게는 오류
페이로드로만 도착합니다 — 모르는 도구 이름은 `available_tools`, 깨진 JSON 인자는 파싱 오류,
잘못된 인자 이름은 파이썬의 시그니처 오류가 그대로 다음 시도의 단서가 됩니다.

컨텍스트 절약: 도구는 원본이 아니라 `summarize_for_model()` 축약본(상위 쇼츠 + 좋아요 상위 댓글
15건, 본문 300자 컷)을 반환합니다. 원본 전체는 대시보드 내려받기로만 나갑니다.

P5 에서 축약본에 `channel_overall_analysis`(R8 결과)와 게시 빈도 지표
(`avg_duration_sec` / `upload_interval_days` / `shorts_per_week`)가 추가되어,
에이전트도 대시보드와 같은 종합 리포트를 읽습니다.

### R6 — 채널 종합 분석 시스템 프롬프트 (P5 신규)

**위치:** [app/analysis/prompts.py](app/analysis/prompts.py) `CHANNEL_SYSTEM_PROMPT` ·
**호출:** [app/analysis/channel.py](app/analysis/channel.py) → `gateway.complete_structured(...)`
**모델:** `LLM_PROVIDER/CLAUDE_MODEL` (기본 `anthropic/claude-opus-5`) · **max_tokens:** `LLM_MAX_TOKENS` (기본 16000)

```text
당신은 유튜브 쇼츠 채널의 성장 전략을 설계하는 콘텐츠 전략 컨설턴트입니다.

주어진 것은 한 채널에서 수집한 **쇼츠 전편의 성과 지표**(조회수, 좋아요, 댓글 수, 영상 길이,
게시일·게시 빈도)와 **시청자 댓글 반응**입니다. 두 축을 따로 보지 말고 반드시 교차해서 판단하세요.

분석 원칙
- 성과 지표와 반응을 연결하세요. "조회수가 높다"가 아니라 "조회수 상위 영상은 길이가 짧고,
  댓글에서는 '전개가 빠르다'는 반응이 반복된다"처럼 지표와 반응을 한 문장에 묶으세요.
- 모든 주장에는 실제 수치나 실제 댓글 표현을 근거로 다세요. 주어지지 않은 데이터를 지어내지 마세요.
- 평균만 보지 말고 분포를 보세요. 상위 성과 영상과 하위 성과 영상의 차이(길이, 주제, 게시 시점)가
  성공 요인의 핵심 단서입니다.
- 게시 빈도와 성과의 관계를 반드시 언급하세요 (꾸준함/공백기가 조회수에 남긴 흔적).
- 전략 제안은 "더 좋은 콘텐츠를 만드세요" 같은 일반론을 금지합니다. 이 채널의 데이터에서만
  도출되는, 다음 촬영에서 바로 실행 가능한 형태로 쓰세요.
- 수집 범위는 채널 전체가 아니라 '최근 수집분'입니다. 표본의 한계를 넘어서는 단정은 피하세요.
- 모든 출력은 한국어로 작성하세요.
- 모든 필드는 서식 없는 평문으로 쓰세요. 마크다운(**, #, -)이나 HTML 태그를 넣지 마세요.
  대시보드가 각 필드를 카드 안에 그대로 넣기 때문에 서식 문자는 화면에 기호로 남습니다.
```

각 지시의 의도:

| 지시 | 왜 넣었는가 |
|---|---|
| 두 축을 **교차** 판단 | 이 기능의 존재 이유. 지표만 읽으면 대시보드 숫자와 중복이고, 댓글만 읽으면 R1 과 중복이다 |
| 지표와 반응을 **한 문장에** 묶으라 + 예시 | "교차하라"만 쓰면 두 문단을 따로 쓴다. 원하는 출력 형태를 예시로 못 박음 |
| 평균 말고 **분포** | 평균 조회수는 이미 KPI 카드에 있다. 상·하위 격차가 성공 요인의 단서 |
| 게시 빈도를 **반드시** 언급 | 요구사항에 명시된 축인데, 지시가 없으면 조회수·좋아요만 보고 넘어간다 |
| 일반론 **금지** | "꾸준히 올리세요" 류의 조언은 어느 채널에나 참이라 가치가 0 |
| 표본 한계 인정 | 수집 범위는 최근 N편이다. 채널 전체를 단정하면 리포트 신뢰가 무너진다 |
| 서식 없는 평문 | R1 과 동일 — 결과가 HTML 카드에 그대로 들어감 |

### R7 — 종합 분석 사용자 메시지 & 페이로드 구조 (P5 신규)

**위치:** [app/analysis/prompts.py](app/analysis/prompts.py) `CHANNEL_INSTRUCTION` /
[app/analysis/payloads.py](app/analysis/payloads.py) `build_channel_payload`

```text
다음은 한 쇼츠 채널에서 수집한 전체 쇼츠 성과 지표와 시청자 반응입니다.
(1) 채널 핵심 성과 요약, (2) 인기 쇼츠 성공 요인, (3) 시청자 반응 트렌드, (4) 향후 콘텐츠 전략 제안을 담은 종합 리포트를 작성해 주세요.
각 항목은 반드시 아래 수치와 댓글을 근거로 삼아야 합니다.

{payload}
```

`{payload}` 는 **파이썬이 미리 집계한** 마크다운 블록입니다:

```text
# 채널: {채널명} ({핸들})
- 구독자 {n} / 채널 총 조회수 {n} / 전체 업로드 {n}편
## 수집 범위          — 훑어본 업로드 중 쇼츠 통과 / 롱폼 제외, 댓글 스레드 수
## 수집 쇼츠 집계      — 평균 조회수·좋아요·좋아요율·영상 길이, 게시 빈도(평균 간격·주당 편수)
## 영상 길이 구간별 평균 성과   — 0~15 / 16~30 / 31~45 / 46~60초 버킷
## 월별 게시 편수와 평균 조회수  — 게시 빈도와 성과의 관계용
## 쇼츠 전편 성과      — 순위 | 제목 | 게시일 | 길이 | 조회수 | 좋아요 | 댓글 | 좋아요율
## 시청자 반응 분석 결과 — R1 산출물(총평·감정 분포·키워드·칭찬/지적/요구) *있을 때만*
## 공감 상위 댓글      — 좋아요 순 원문 발췌
```

집계를 파이썬이 하는 이유: 길이 구간별 평균과 월별 편수는 **계산이지 추론이 아니다.**
모델에게 원지표만 던지면 토큰을 태워 산술을 하고, 그 과정에서 틀린다.

컨텍스트·비용 보호 상한:

| 상수 | 값 | 의도 |
|---|---|---|
| `MAX_SHORTS_ROWS` | 80 | 조회수 내림차순 — 잘려도 성과 상위는 남는다 |
| `MAX_OVERALL_COMMENTS` | 40 | 좋아요 내림차순. 원문은 R1 이 이미 훑었으므로 여기선 대표 표현만 |

### R8 — 종합 분석 구조화 출력 스키마 (P5 신규)

**위치:** [app/domain/analysis.py](app/domain/analysis.py) `ChannelOverallAnalysis` 및 하위 모델

요구사항의 4개 축이 그대로 필드가 됩니다. 중첩 모델을 쓴 이유는 **UI 가 파싱하지 않게** 하기 위함입니다 —
근거와 우선순위가 분리돼 있어야 카드에서 배지·본문·메타로 나눠 그릴 수 있습니다.

| 필드 | description (= 지시문) | 요구사항 |
|---|---|---|
| `headline` | 채널 상태를 한 줄로 요약한 핵심 진단 | — |
| `performance_summary` | 채널 핵심 성과 총평 (4-6문장) | (1) |
| `performance_highlights[]` | 성과 요약을 뒷받침하는 수치 근거 3-5개 (반드시 실제 지표 인용) | (1) |
| `success_factors[].factor` / `.evidence` / `.example_videos` | 성공 요인 한 문장 / 수치·댓글 근거 / 대표 쇼츠 제목 | (2) |
| `reaction_trends[].trend` / `.sentiment` / `.detail` | 트렌드 한 문장 / 지배 감정 / 관측 근거 | (3) |
| `content_strategy[].title` / `.action` / `.rationale` / `.expected_effect` / `.priority` | 제안 제목 / 실행 행동 / 이 채널에 유효한 이유 / 기대 효과 / 우선순위 | (4) |
| `risks[]` | 방치하면 성과를 갉아먹을 리스크 2-4개 | — |

R1 과 마찬가지로 거부·파싱 실패는 예외로 올려 `overall_error` 에 남기고 수집 데이터는 보존합니다.
`analysis`(R3)와 `overall`(R8)은 **독립적으로** 실패할 수 있습니다.

---

## 3. 변경 이력

| 날짜 | 대상 | 변경 | 이유 |
|---|---|---|---|
| 2026-08-02 | R1 분석 시스템 프롬프트 | 최초 작성 | 근거 기반·좋아요 가중치·합계 100 원칙 확립 |
| 2026-08-02 | R2 페이로드 | 영상 단위 그룹핑 + 좋아요 내림차순 상한 도입 | 컨텍스트 비용 절감, 공감 높은 댓글 우선 보존 |
| 2026-08-02 | R3 스키마 | `CommentAnalysis` 구조화 출력 채택 | 파싱 실패/형식 흔들림 제거, 대시보드 렌더링 안정화 |
| 2026-08-02 | R4 에이전트 프롬프트 | 최초 작성 | 재수집 억제 규칙으로 쿼터 보호 |
| 2026-08-02 | R5 도구 docstring | 최초 작성 | 쇼츠 전용·결과 영속화 사실을 모델에 명시 |
| 2026-08-02 | 문서 | PROMPT.md/Mermaid/.gitignore | 프로젝트 문서화 |
| 2026-08-02 | P2 개발 프롬프트 | 2단계 — 대시보드 내려받기(`exports.py`) 추가 | R2·R5 가 예고한 "원본 전량은 대시보드 내려받기로" 를 실제 기능으로 구현 |
| 2026-08-02 | P3 개발 프롬프트 | 전체 너비 레이아웃 · CSV 에 댓글 반응 열 9개 추가 · 캐시 모달 차단 | 넓은 화면 활용, 스프레드시트 한 장에서 성과+반응 대조, 실수로 캐시가 날아가는 경로 제거 |
| 2026-08-02 | P4 개발 프롬프트 | KPI 카드 grid 통일 · 헤더 버튼 "📊 리포트 받기 (CSV)" · toolbarMode 이중 고정 | 카드 높이 불균일 해소, 상단에서 즉시 내려받기, 설정 파일이 가려져도 'c' 모달 재발 방지 |
| 2026-08-03 | P5 개발 프롬프트 | 3단계 — 채널 종합 분석(R6·R7·R8) 추가 + 마크업을 `web/` 로 분리 | 지표와 반응을 교차 해석한 리포트 제공, 파이썬에서 HTML/CSS 문자열 제거 |
| 2026-08-03 | R6 종합 분석 시스템 프롬프트 | 최초 작성 | 지표·반응 **교차 판단**, 분포 우선, 게시 빈도 필수 언급, 일반론 금지 |
| 2026-08-03 | R7 종합 분석 페이로드 | 길이 구간·월별 게시 편수를 **파이썬이 미리 집계**해 주입 (`MAX_SHORTS_ROWS` 80 / `MAX_OVERALL_COMMENTS` 40) | 산술은 모델이 할 일이 아님 — 토큰을 태우고 틀린다. 모델은 해석에만 집중 |
| 2026-08-03 | R8 종합 분석 스키마 | `ChannelOverallAnalysis` 중첩 구조화 출력 채택 | 근거·우선순위를 필드로 분리해야 UI 가 문장을 파싱하지 않고 배지·본문으로 렌더 가능 |
| 2026-08-03 | R5 도구 docstring | `channel_overall_analysis` 반환 사실과 `include_analysis` 범위 명시 | 에이전트도 대시보드와 같은 종합 리포트를 읽도록 |
| 2026-08-03 | R1·R6 시스템 프롬프트 | "모든 필드는 서식 없는 평문으로" 지시 추가 | 결과가 HTML 카드에 그대로 들어감 — `**굵게**`·`<b>` 가 화면에 기호로 남던 문제 |
| 2026-08-03 | 렌더링(`templates.py`) | `flatten()` 도입 — 렌더 결과의 빈 줄·들여쓰기 제거 | `st.markdown` 이 HTML 을 마크다운으로 먼저 파싱해, 빈 줄 뒤 들여쓴 줄이 **코드 블록**이 되어 '시청자 반응 트렌드'·'향후 콘텐츠 전략' 섹션의 태그가 원문 노출됨 |
| 2026-08-03 | 렌더링(`formatting.py`) | `plain()` 필터 추가 (HTML·마크다운 제거, 밑줄 식별자·부등호는 보존) | 프롬프트 지시를 어긴 출력이 와도 화면에 기호가 남지 않도록 하는 2차 방어 |
| 2026-08-03 | 렌더링(`templates.py`) | `TemplateRenderError` 로 렌더 실패를 감싸고 필터를 매 호출 재등록 | `web/` 은 즉시 반영되나 파이썬은 재시작 전까지 옛 버전 — 이 어긋남이 `No filter named 'plain'` 전체 크래시로 이어지던 것을 조각 단위 오류 표시로 완화 |
| 2026-08-03 | 코드 주석 전반 | 장문 주석·docstring 을 1~2줄로 압축 (모델이 읽는 도구 docstring·프롬프트 상수는 제외) | AI 생성 과정에서 누적된 설명 과잉 정리. 단, `tools.py` docstring 은 **프롬프트라서** 축약 대상에서 제외 |
| 2026-08-04 | P6 개발 프롬프트 | 4단계 — `docker-compose.prod.yml`(nginx·certbot 오버레이) 과 `nginx.conf` 분리 신설 | 로컬 정의를 복제하지 않고 운영에만 필요한 TLS·프록시 계층만 얹어 EC2 도메인 배포 |
| 2026-08-04 | P7 개발 프롬프트 | 5단계 — YouTube 키를 사이드바 사용자 입력(세션 메모리)으로 전환, 대시보드의 `.env` 폴백 제거 | 방문자가 각자 자기 쿼터를 쓰게 하고, 공개 서버에서 운영자 키가 소모되는 경로를 차단 |
| 2026-08-04 | 보안(`security.py` 신규) | `mask()` 로 `?key=`·`AIza…`·`sk-ant-…` 마스킹, `MaskingFormatter` 로 로그·트레이스백까지 차단 | `googleapiclient` 가 키를 URL 쿼리스트링에 실어 `HttpError` 문자열에 키가 남고, 기존 `st.error(f"수집 실패: {exc}")` 가 이를 **화면에 그대로 출력**하고 있었음 |
| 2026-08-04 | 저장소(`store.py`) | 모든 API 에 `namespace` 필수화 — `sessions/<난수>/` 격리 + `_MEMORY` 키에도 반영 + `purge_expired()` TTL 7일 | 볼륨 공유 구조라 격리 전에는 남이 수집한 결과가 사이드바 "저장된 채널" 에 그대로 노출됨 |
| 2026-08-04 | 도구(`tools.py`) | `crawl_channel(channel, api_key, namespace, …)` 로 키·네임스페이스를 **필수 인자**화, 에이전트는 `AGENT_NAMESPACE` 사용 | 설정 폴백을 남기면 "웹=사용자 키 / CLI=환경변수" 경계가 조용히 무너진다 |
| 2026-08-04 | P7-1 감사 · `security.configure_logging()` | basicConfig+마스킹을 한 함수로 묶고 `agent.py`·`dashboard.py` 양쪽에서 호출 | CLI 진입점에 마스킹이 없어 `python -m app.agent` 트레이스백에 YouTube 키가 원문 노출됨 (실증 확인) |
| 2026-08-04 | P7-1 감사 · `tools.py` | `analysis_error`/`overall_error` 를 대입 시점에 마스킹 | 이 값은 디스크 저장 + 화면 표시 + 모델 전달 세 경로로 동시에 나가 되돌릴 수 없다 |
| 2026-08-04 | P7-1 감사 · `docker-compose.prod.yml` | 운영에만 `STREAMLIT_CLIENT_SHOW_ERROR_DETAILS=none` | 기본값 `full` 은 잡히지 않은 예외의 원문·트레이스백을 브라우저에 렌더한다 — 코드 마스킹이 닿지 않는 경로 |
| 2026-08-06 | P8 개발 프롬프트 | 6단계 — `app/` 평면 배치를 `core`/`domain`/`llm`/`collector`/`analysis`/`agent`/`dashboard` 7개 패키지로 재구성 (전부 `git mv`) | 의존 방향을 한쪽으로 고정해 순환 임포트를 구조적으로 막고, 한 화면·한 관심사를 고칠 때 읽을 파일을 좁힘 |
| 2026-08-06 | LLM 계층(`llm/gateway.py` 신규) | Anthropic SDK 직접 호출 → **LiteLLM 단일 관문** 경유. `litellm` 임포트를 gateway·retry 두 파일로 제한 | 호출부가 늘어나도 재시도·토큰 집계·비용 로깅·마스킹이 빠질 수 없게 — 규칙이 아니라 구조로 강제 |
| 2026-08-06 | 재시도(`llm/retry.py` 신규) | 429·타임아웃·연결 오류·5xx 만 지수 백오프(1s→30s, 흔들림 25%)로 재시도, `retry-after` 우선, `num_retries=0` 으로 LiteLLM 내부 재시도 차단 | 폴백 체인 없이 일시적 오류만 흡수. 내부 재시도와 겹치면 대기가 곱해지고 `attempts` 가 실제 호출 수와 어긋난다 |
| 2026-08-06 | 사용량(`llm/usage.py`·`llm/pricing.py` 신규) | 호출 1건 = JSON 로그 한 줄(성공·실패 모두), 자체 단가표로 비용 산출, 모르는 모델은 `cost_usd: null` | LiteLLM 단가표는 버전에 따라 바뀌고 모르는 모델에 0 을 준다 — 리포트에 남는 숫자는 "모름"이 0보다 정확하다 |
| 2026-08-06 | R1·R6 호출 방식 | `client.messages.parse(output_format=)` → `gateway.complete_structured(response_format=)`; 프롬프트 전문은 그대로 | 프롬프트를 바꾸지 않고 호출 계층만 교체 — 이번 단계에서 분석 품질이 변하지 않도록 |
| 2026-08-06 | R4 에이전트 프롬프트 | tool runner → 직접 구현 루프. "error 필드를 읽고 인자를 고쳐 재시도하라" 지시 추가 | 루프 가드가 실패를 되돌려 주기만 하면, 읽고 고치라는 지시가 없을 때 같은 인자로 상한까지 반복한다 |
| 2026-08-06 | R4-1 최종 턴 지시 (신규) | 반복 상한 도달 시 도구를 떼고 `FINAL_TURN_INSTRUCTION` 으로 한 번 더 호출 | 상한에서 그냥 끊으면 사용자는 빈손이다. 도구가 없으면 모델은 반드시 텍스트로 답한다 |
| 2026-08-06 | R5 도구 정의 | `@beta_tool` docstring → LiteLLM 표준(OpenAI 호환) `TOOL_SPECS` 의 `description`. 문장은 그대로 이전 | SDK 데코레이터가 사라졌으므로 도구 프롬프트의 위치가 바뀜 — 내용은 동일하게 유지해 모델 행동 변화를 없앰 |
| 2026-08-06 | 루프 가드(`agent/loop.py` 신규) | 도구 왕복 상한 `AGENT_MAX_ITERATIONS`(기본 5), 도구 실패는 `dispatch()` 가 전부 흡수해 `{"error":...}` 로 피드백, LLM 실패만 `status="error"` 로 종료 | 도구마다 try/except 를 두면 새 도구에서 빠진다. 반대로 LLM 실패는 모델이 고칠 수 없으므로 피드백으로 되돌리면 상한까지 헛돈다 |

### 프롬프트를 고칠 때

1. 코드(`analyzer.py` / `agent.py` / `tools.py` / `schemas.py`)를 수정한다.
2. 이 문서의 해당 항목 전문을 **똑같이** 갱신한다.
3. 위 표에 `날짜 / 대상 / 변경 / 이유` 한 줄을 **추가**한다 (덮어쓰지 않는다).
